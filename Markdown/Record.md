
## Greedy

#### CF2055E
* 可以考虑钦定顺序的答案
* 贪心涉及到与 $0$ 取 $\max$ 一类，可能忽略这条直接贪是对的
#### luoguP3462
* 贪心可以反向扫然后每次不能假如就置换掉当前最劣的
## Constructive

#### CF2062F
* $\max(x,y)=\frac{(x+y)+|x-y|}{2}$
#### CF2061G
* 两种颜色的 $n$ 条边，颜色交互器确定，同色匹配最大大小为 $\lfloor \frac{(n+1)}{3} \rfloor$，构造方案考虑构造一条同色链 $a_1 \cdots a_k$，和多个异色三元环 $(u_i,v_i,w_i)$，每个点属于其中一种，容易证明答案合法
### xyd 19445
* $n = 2^k$ 的时候可以考虑拆位
* 形如 $\text{popcnt(x)} = \frac b 2$ 的二进制数在构造时可能比较有用 $\rightarrow$ \[KTSC\] 魔法师
#### CF1481F
* 答案下界易得的题目不妨考虑去构造答案上界
#### CF1012E
* 一种使用两次子序列的排列的循环移动的将数组排序的方案:
	Step 1: 对所有置换环连载一起进行一次操作，此时每一组恰有一个数错位到上一个置换环
	Step 2: 再把错位的跑一次置换
#### CF1270G
* 形如 $b_i-i$ 求和为 $0$ 的构造可以建图找环
## DP

#### xyd19366
* 多起点单终点这一类的问题的优化也可以隐式地用在 DP 优化上
#### xyd 19373
* 大小为 $a_1 \dots a_n$ 的连通块合并为一棵树的方案数为 $\prod a_i \left(\sum a_i\right)^k$.
* 关于值域的答案为多项式的 dp 可以 lagrange 插值求解
#### CF1517F
* DP 的多个维度在两种不同的条件下只有一个有用，所以拆成两个 DP
#### CF1608F
* $\text{MEX}$ 类的 DP，大于当前 $\text{MEX}$ 的可以在用到的时候再分配值
#### CF2061I
* 当部分情况的信息可以快速处理，另外一部分信息有关序列长度时可以分块求解
#### CF2079B
* DP 多起点单终点改贪心+DS 优化
#### LuoguP7155
* 从高位到低位划分过程然后 DP
#### LibreOJ2731 / CF1060F
* 对于一类依靠相邻信息的计算操作顺序数量计数 DP，状态设计可以为 $f(u,i,\dots)$ 表示在 $u$ 对应的子状态里，$u$ 在第 $i$ 次操作后 ...
## Interactive / Communication

#### QOJ6661
* 多次操作机会可以迭代
* $w = u \oplus v$，$2^{\text{highbit}(w)+1} + (v \land 2^{(\text{highbit}(w))})$ 可以保证在 $u,v$ 不同的情况下，对 $u,v$ 和 $v,w$ 算出来的结果不同
#### xyd19367
* 处理关于在子树节点集合的问题时从叶到根处理会方便很多
#### Kattis GuessingGame2
* 通信题一定要考虑你给的方案能不能存储更多信息去优化，整体结构上的东西也可以存
#### CF1906C
* 交互构造 "被询问" 的东西时可以考虑构造影响答案的位置的数量很少的方案以确定询问的信息
#### CF1028G
* 交互题要求询问数量极限最优化，且询问的范围依赖前几次询问的信息，考虑 DP 去求解方案
#### CF1081F
* 你给定 $l, r$ 随机 $[1,r]$ 和 $[l,n]$ 执行可逆操作 (e.g. `xor`) 并得到全局信息，期望询问 $[i,i]$ $8$ 次即可得到仅操作 $i$ 的全局信息
#### CF1447E
* 询问多组无交的对的颜色相同情况 (RGB)，则可以用 $2$ 次操作求解颜色段，然后 $3$ 次操作求出间隔 $1$ 颜色段的关系，$5$ 次询问
#### CF1406E
* 根号分块平衡交互询问次数
* $2$ 次操作 (一次询问，一次查询) 求值 ($1$ 个数) 可以根号分块优化到 $n+2\sqrt n$ 次
#### CF1510I
* ML 结论: 选择题贺答案，根据以往的回答确定每个人的准确率 (指数函数)，按照准确率随机去贺，错误的数量是最牛的人错误量的约 $1.3$ 倍以内
#### CF1557E
* 棋盘上抓棋子，考虑按行扫并且考虑被抓的人的某个方向的移动次数上限
#### AtCddcc2020_qual_e
* 零点存在性定理可以二分
#### CF1761G1/G2/G3
* 询问 $\left|\left\{\lfloor\frac{p_i}{2} | i \in [l,r] \cap \mathbb{Z} \rfloor\right\}\right|, \text{p is a permutation}$ 等价于询问区间 $[l,r]$ 中的除以二下取整相同的匹配数 
## Strings

#### CF1483F
* 对可能的答案集合判断全部合法这一步转计数，即对于串 $s$，备选串 $s_i$ 合法当且仅当 $s_i$ 在 $s$ 中的出现次数 $f_i$ $=$ $s_i$ 在 $s$ 中的 **作为备选串且不被其他备选串覆盖** 的出现次数 $g_i$
* 可行性问题可以通过答案的性质转化为计数问题
## Data Structures

#### QOJ8672
* 扫描线可以直接维护每个询问的答案
#### CF1178G
* 求 $a \cdot b$ 的最值，动态修改一个值，另一个值给定，当成 $kx$ 使用凸包维护
* 区间分块维护凸包加，单块的指针移动次数至多 $O(\sqrt n)$ 次
## Number Theory
#### CF809E
* $\varphi(x\cdot y)=\frac{\varphi(x)\cdot \varphi(y)\cdot \gcd(x,y)}{\varphi(\gcd(x,y))}$  
#### CF1225G
* 给定 $k$ 使得将 $x$ 和 $y$ 合并为 $\frac{(x+y)}{f(x+y)}$ $f(x)=k^x, k=\max\{i \in \mathbb{N} | (k^i | x)\} \Longleftrightarrow$ $\exists b=\mathbb{N}^n, \sum a_ik^{-b_i}=1$
#### 7fa4Q4.1.2.1
* 约数和为 $S$ 的数大小是 $O(S)$ 的
#### AtCarc136E
* $x|y \Longleftrightarrow x \rightarrow y$ 的 DAG 可达性讨论 $\text{minP}$
## Math
#### AtCagc019F
* 贪心最优化答案的答案和计数题考虑转化题意把价值分摊到每一步
* 组合意义，对于对角线有明显意义的方格图计数考虑对折后分开计算
### Problems
* 枚举数的 $k$ 次方考虑转第二类斯特林数
#### AtCagc058D
* 容斥可以只容斥起点
## Polys and GFs
#### CF933D
* 不幸遇到很难的式子题，不过答案可以证是多项式可以考虑暴力求解几项然后 Lagrange 插值
#### CF891E
- $$
 \sum\limits_{i\ge 0} (a_i-i)\frac{x^i}{i!}=\sum\limits_{i \ge 0} a_i\frac{x^i}{i!}-x\sum\limits_{i \ge 0} \frac{x^i}{i!}=a_ie^x-xe^x
 $$
## Combination
#### LuoguP5405
* 对于边定向的容斥：若边的朝向固定可以求解 (正边)，则可以将反边看作不存在 / 正边，然后容斥 
#### LuoguP8114
* 三角形匹配 $\longrightarrow$ 三维堆叠
* 每一维均有单调性的三位堆叠的方案数可以用 LGV 求解每一条划分高度的线的连法
#### AtCagc060D / LuoguP8329
* 容斥套容斥化简
## Geometry

## Graph
#### CF1906I
* 性质: Top 序为 $p_1 \dots p_n \Rightarrow \forall i \in [1, n - 1] (p_i,p_{i+1}) \in G$
#### QOJ8673
* 随机图的最短路树等生成树高度都是 $O(\log n)$ 可以乱做
* 对于这种深度不大的题目可以考虑使用双向搜索的思想 QwQ
* 随机图双端 dij 的期望访问点数是 $O(\sqrt n)$ 的
#### CF718E
* 分类的求类最短路的计数问题，可以考虑分别计算点到集合和集合到集合
#### AtCagc067A
* 最大团大小 $=$ 反图上的最大独立集大小
## Network Flow
#### CF1704E
* 每秒退流一次 $\longrightarrow$ 先退流 $n$ 秒
#### CF1307G
* 增加边的边权使最短路尽量长一定只改最小割割边
#### CF1404E
* 木板覆盖空地且不可重，点边互换
* 可重 ([P6062 [USACO05JAN] Muddy Fields G - 洛谷](https://www.luogu.com.cn/problem/P6062)) 将空地的横纵坐标连接，最大匹配
#### CF1510B
* DAG 上路径覆盖可以拆点转化为二分图匹配
#### CF2062G
* 交换两个位置可以看成两组形如点对 $(a_i,i)$ 的匹配
* 计算答案下界的方式可以通过均摊代价
#### AtCarc101C / UOJ951
* 大小为 $2x$ 的团的匹配数是 $\prod\limits_{i=1}^x (2i-1)$
* 大小为 $n$ 的团选 $i$ 组匹配的方案数是 $\frac{n!}{i!(n-2i)!2^i}$
## Games

#### CF2062E2
* 博弈的必胜计数可以考虑求解必败局面
#### CF1033G
* A 和 B 的操作不同，对每种操作方式胜者的计数，A 和 B 的答案相同，和先/后手胜做容斥
* 取石子，A 去 $a$ 个, B 取 $b$ 个，则石子数 $v$ 与 $v \bmod (a+b)$ 等价
#### CF1536F
* 必胜策略是确定的
#### AtCagc002e
* 有序高度序列，每次删除最下面一行或最右侧一列的必胜策略，等价于每次往上或往右走一步
## Bitmasks

#### xyd 19443
* 以某个数以位运算生成序列可以考虑处理原数 $x$ 的每一位的贡献
#### CF29D
* 区间异或可以转化为在异或差分序列上异或 $1$
* 把空序列转化为目标序列 $\Longleftrightarrow$ 把目标序列删空
## Misc
#### xyd 19446
* 遇到双变量的东西可以考虑去改单变量去优化
#### xyd 19459
* 可以通过给定的信息 ($x \in S_i$) 确定 $x$ $\longrightarrow$ 包含 $x$ 的 $S_i$ 集合唯一
#### CF933E
* 将 $x_i, x_{i+1}$ 同时减去 $\min(x_i,x_{i+1})$ 的代价是 $\min(x_i,x_{i+1})$ 的情况下，将正整数序列操作到正整数不连续，则操作后的序列中连续 $0$ 的数量不会超过 $2$
#### CF1051G
* 序列上的 $len > 1$ 连续段移除最大值改为最小值 $-1$ 和最小值改为最大值 $+1$ 的方案，若操作 $1$ 的代价为负，操作 $2$ 的代价为正，则新加一个数后，操作后的答案集合唯一确定
#### CF663E
* 异或求最少剩余 $1$ $\Longrightarrow$ 反转尽量少的位使得可以变化成全 $0$
#### CF1523G
* 调和级数分析精细实现的函数的调用次数
#### CF1621H
* 树上分区
#### CF1379F1/F2
* 棋盘上 $2\times2$ 的区域最多放 $1$ 个 $\color{gold}{\text{K}}$$\text{ing}$
#### CF878D
* 小值域 $0 \sim k-1$ 求最值拆成 $k$ 个组
* 压缩等价类维护
#### AtCabc326F
* $n$ 小 $V$ 大的背包可以折半搜索
#### LuoguP10149
* 区间计数转两个前缀计数的差维护
#### UOJ950
* 对于一类小球往 左/右 滚动并改变格子符号的问题，注意观察性质和找不变量
#### UOJ951
* 关心 $a_i+a_j \le w$ 的数对数量的计数，分讨 $a_i \le \lfloor \frac w 2 \rfloor$ 和 $a > \lfloor \frac w 2 \rfloor$ 
#### LuoguP3713
* 方案数平方等价于两个人选同一个方案的选法
#### LuoguP5396
* 对于某个数列，$a_1 \dots a_k$ 可以成为它的最大前缀和的充要条件是 $a_1 \dots a_k$ 所有后缀和非负且 $a_{k+1}\dots a_n$ 的所有前缀和非正 
#### CF1605F
* 使用极长合法序列生成不合法序列然后容斥

## Randomize
#### CF1578M
* 保证数据随机且只需要给出正确率较高的方案，本地随机生成数据优化参数 (ML ???)
## Users
[经典 trick 选记 - lsj2009 - 博客园](https://www.cnblogs.com/lsj2009/p/17668670.html)






