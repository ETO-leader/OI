#include<bits/stdc++.h>
#define cir(i,a,b) for(int i=a;i<b;++i)
using namespace std;
using lint=long long;
using VI=vector<lint>;
//auto init_c(int x){
//    vector<VI> res(x,VI(x));
//    res[0][0]=1;
//    cir(i,1,x) cir(j,0,x){
//        if(j) res[i][j]+=res[i-1][j-1];
//        res[i][j]+=res[i-1][j];
//    }
//    return res;
//}
int main(){
    ios::sync_with_stdio(false),cin.tie(0);
    int n,k;cin>>n>>k;
    vector<vector<VI>> D(2,vector<VI>(n+7,VI(k)));
    
    return 0;
}
